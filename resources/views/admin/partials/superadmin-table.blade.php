<section class="overflow-hidden rounded-2xl border border-outline-variant bg-white">
    <div class="border-b border-outline-variant/50 px-6 py-5"><h3 class="text-2xl font-bold">{{ $title }}</h3><p class="mt-1 text-sm text-on-surface-variant">{{ $items->count() }} élément(s) enregistré(s)</p></div>
    <div class="overflow-x-auto"><table class="w-full min-w-[700px] text-left"><thead class="bg-surface-container-low text-xs uppercase tracking-wide text-on-surface-variant"><tr>@foreach ($columns as $column)<th class="px-6 py-3">{{ str_replace('_', ' ', $column) }}</th>@endforeach</tr></thead><tbody class="divide-y divide-outline-variant/30">@forelse ($items as $item)<tr class="hover:bg-surface-container-low">@foreach ($columns as $column)<td class="px-6 py-4 text-sm">{{ $column === 'est_approuve' ? ($item->$column ? 'Approuvé' : 'En attente') : ($item->$column ?? '—') }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($columns) }}" class="px-6 py-10 text-center text-sm text-on-surface-variant">Aucune donnée disponible.</td></tr>@endforelse</tbody></table></div>
</section>
